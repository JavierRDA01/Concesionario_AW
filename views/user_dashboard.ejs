<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <title>Mi Dashboard - WibbleUCM</title>
</head>
<body class="bg-light">
    
    <%- include('./barraNavegadora.ejs') %>

    <div class="container-fluid my-4">
        <div class="row">
            
            <div class="col-md-2 d-none d-md-block bg-white sidebar p-3 shadow-sm rounded me-3" style="min-height: 80vh;">
                <h5 class="text-muted">Mi Espacio</h5>
                <hr>
                <ul class="nav flex-column">
                    <li class="nav-item mb-2">
                        <a href="/users/dashboard" class="nav-link active text-danger fw-bold bg-light rounded">
                            <i class="bi bi-speedometer2"></i> Mi Dashboard
                        </a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/vehicles" class="nav-link text-dark">
                            <i class="bi bi-ev-front"></i> Vehículos Disponibles
                        </a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/reservations/new" class="nav-link text-dark">
                            <i class="bi bi-plus-circle"></i> Nueva Reserva
                        </a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/my-reservations" class="nav-link text-dark">
                            <i class="bi bi-journal-text"></i> Historial
                        </a>
                    </li>
                </ul>
            </div>

            <main class="col-md-9 ms-sm-auto col-lg-9 px-md-4">
                
                <div class="card border-0 shadow-sm bg-white mb-4">
                    <div class="card-body p-4 d-flex justify-content-between align-items-center flex-wrap">
                        <div>
                            <h2 class="display-6 m-0">¡Hola, <span class="text-danger fw-bold"><%= user.nombre %></span>!</h2>
                            <p class="lead text-muted mb-0">Bienvenido a tu panel de movilidad sostenible.</p>
                        </div>
                        <div class="mt-3 mt-md-0">
                            <a href="/reservations/new" class="btn btn-danger shadow-sm">
                                <i class="bi bi-plus-lg"></i> Reservar Ahora
                            </a>
                        </div>
                    </div>
                </div>

                <div class="row g-4">
                    
                    <div class="col-md-4">
                        <div class="card h-100 shadow-sm border-start border-danger border-4">
                            <div class="card-body">
                                <h5 class="card-title text-danger"><i class="bi bi-geo-alt-fill"></i> Mi Base Asignada</h5>
                                <div class="mt-3">
                                    <% if(miConcesionario) { %>
                                        <h4 class="mb-1"><%= miConcesionario.nombre %></h4>
                                        <p class="text-muted small mb-3">
                                            <i class="bi bi-pin-map"></i> <%= miConcesionario.direccion %><br>
                                            <%= miConcesionario.ciudad %>
                                        </p>
                                    <% } else { %>
                                        <p class="text-muted">No tienes concesionario asignado.</p>
                                    <% } %>
                                </div>
                                <div class="mt-auto">
                                    <a href="/vehicles" class="btn btn-outline-danger btn-sm w-100">Ver flota de mi base</a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="card h-100 shadow-sm border-start border-danger border-4">
                            <div class="card-body">
                                <h5 class="card-title text-danger"><i class="bi bi-activity"></i> Estado Actual</h5>
                                <div class="mt-3">
                                    <% if(reservasActivas.length > 0) { %>
                                        <div class="alert alert-success border-success mb-0">
                                            <strong class="d-block mb-1"><i class="bi bi-check-circle-fill"></i> Tienes una reserva activa</strong>
                                            
                                            <h5 class="mb-0 mt-2"><%= reservasActivas[0].marca %> <%= reservasActivas[0].modelo %></h5>
                                            <small class="text-muted"><%= reservasActivas[0].matricula %></small>
                                            
                                            <hr class="my-2">
                                            <div class="d-flex justify-content-between small">
                                                <span>Devolución:</span>
                                                <strong><%= new Date(reservasActivas[0].fecha_fin).toLocaleDateString() %></strong>
                                            </div>
                                        </div>
                                    <% } else { %>
                                        <div class="text-center py-3 text-muted">
                                            <i class="bi bi-car-front display-4 mb-2 d-block text-secondary opacity-25"></i>
                                            <p class="mb-0">No tienes vehículos en uso actualmente.</p>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="card h-100 shadow-sm border-start border-secondary border-4">
                            <div class="card-header bg-white border-bottom-0 pt-3 pb-0">
                                <h5 class="card-title text-dark mb-0"><i class="bi bi-clock-history"></i> Actividad Reciente</h5>
                            </div>
                            
                            <div class="card-body p-0">
                                <ul class="list-group list-group-flush">
                                    <% if(ultimasReservas.length > 0) { %>
                                        <% ultimasReservas.forEach(r => { %>
                                            <li class="list-group-item border-0 py-3">
                                                <div class="d-flex align-items-center">
                                                    <div class="flex-shrink-0 me-3">
                                                        <% if(r.imagen && r.imagen !== 'default_car.jpg') { %>
                                                            <img src="/img/<%= r.imagen %>" alt="Coche" class="rounded" style="width: 45px; height: 45px; object-fit: cover;">
                                                        <% } else { %>
                                                            <div class="bg-light rounded d-flex align-items-center justify-content-center" style="width: 45px; height: 45px;">
                                                                <i class="bi bi-car-front-fill text-secondary"></i>
                                                            </div>
                                                        <% } %>
                                                    </div>
                                                    
                                                    <div class="flex-grow-1 min-width-0">
                                                        <h6 class="mb-0 text-truncate"><%= r.marca %> <%= r.modelo %></h6>
                                                        <small class="text-muted"><%= new Date(r.fecha_inicio).toLocaleDateString() %></small>
                                                    </div>

                                                    <div class="ms-2">
                                                        <% if(r.estado === 'activa') { %>
                                                            <span class="badge bg-success rounded-pill">Activa</span>
                                                        <% } else if(r.estado === 'finalizada') { %>
                                                            <span class="badge bg-secondary rounded-pill">Fin</span>
                                                        <% } else { %>
                                                            <span class="badge bg-danger rounded-pill">Cancel</span>
                                                        <% } %>
                                                    </div>
                                                </div>
                                            </li>
                                        <% }) %>
                                    <% } else { %>
                                        <li class="list-group-item text-center text-muted py-4 border-0">
                                            No hay actividad reciente.
                                        </li>
                                    <% } %>
                                </ul>
                            </div>
                            <div class="card-footer bg-white border-top-0 text-center pb-3">
                                <a href="/my-reservations" class="text-decoration-none fw-bold text-danger small">Ver historial completo <i class="bi bi-arrow-right"></i></a>
                            </div>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>