<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <title>Mi Perfil - WibbleUCM</title>
</head>
<body class="bg-light">
    
    <%- include('./barraNavegadora.ejs') %>

    <div class="container-fluid my-4">
        <div class="row">
            
            <div class="col-md-2 d-none d-md-block bg-white sidebar p-3 shadow-sm rounded me-3" style="min-height: 80vh;">
                <% if (user.rol === 'admin') { %>
                    <h5 class="text-muted">Menú Admin</h5>
                    <hr>
                    <ul class="nav flex-column">
                        <li class="nav-item mb-2"><a href="/admin/dashboard" class="nav-link text-dark"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
                        <li class="nav-item mb-2"><a href="/admin/vehicles" class="nav-link text-dark"><i class="bi bi-car-front"></i> Gestión de Flota</a></li>
                        <li class="nav-item mb-2"><a href="/admin/users" class="nav-link text-dark"><i class="bi bi-people"></i> Usuarios</a></li>
                        <li class="nav-item mb-2"><a href="/admin/reservations" class="nav-link text-dark"><i class="bi bi-calendar-check"></i> Reservas</a></li>
                    </ul>
                <% } else { %>
                    <h5 class="text-muted">Mi Espacio</h5>
                    <hr>
                    <ul class="nav flex-column">
                        <li class="nav-item mb-2"><a href="/users/dashboard" class="nav-link text-dark"><i class="bi bi-speedometer2"></i> Mi Dashboard</a></li>
                        <li class="nav-item mb-2"><a href="/vehicles" class="nav-link text-dark"><i class="bi bi-ev-front"></i> Vehículos Disponibles</a></li>
                        <li class="nav-item mb-2"><a href="/reservations/new" class="nav-link text-dark"><i class="bi bi-plus-circle"></i> Nueva Reserva</a></li>
                        <li class="nav-item mb-2"><a href="/my-reservations" class="nav-link text-dark"><i class="bi bi-journal-text"></i> Historial</a></li>
                    </ul>
                <% } %>
            </div>

            <main class="col-md-9 ms-sm-auto col-lg-9 px-md-4">
                <div class="row justify-content-center">
                    <div class="col-12 col-xl-10">
                        
                        <div class="d-flex align-items-center mb-4">
                            <div class="bg-danger text-white rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 60px; height: 60px;">
                                <i class="bi bi-person-lines-fill fs-2"></i>
                            </div>
                            <div>
                                <h2 class="text-dark mb-0">Mi Perfil</h2>
                                <p class="text-muted mb-0">Gestiona tu información personal y preferencias.</p>
                            </div>
                        </div>

                        <div class="card shadow-sm border-top border-danger border-4">
                            <div class="card-body p-4">
                                <div class="row">
                                    <div class="col-md-6 border-end">
                                        <h5 class="text-danger mb-3"><i class="bi bi-person"></i> Datos Personales</h5>
                                        
                                        <div class="mb-3">
                                            <label class="small text-muted text-uppercase fw-bold">Nombre Completo</label>
                                            <p class="fs-5 fw-bold text-dark"><%= usuarioDetalle.nombre %></p>
                                        </div>
                                        
                                        <div class="mb-3">
                                            <label class="small text-muted text-uppercase fw-bold">Correo Electrónico</label>
                                            <p class="fs-5"><%= usuarioDetalle.correo %></p>
                                        </div>

                                        <div class="mb-3">
                                            <label class="small text-muted text-uppercase fw-bold">Teléfono de Contacto</label>
                                            <p class="fs-5"><%= usuarioDetalle.telefono || 'No especificado' %></p>
                                        </div>
                                    </div>

                                    <div class="col-md-6 ps-md-4">
                                        <h5 class="text-danger mb-3"><i class="bi bi-building"></i> Datos Corporativos</h5>
                                        
                                        <div class="mb-3">
                                            <label class="small text-muted text-uppercase fw-bold">Rol Asignado</label>
                                            <div class="mt-1">
                                                <% if(usuarioDetalle.rol === 'admin') { %>
                                                    <span class="badge bg-danger fs-6 px-3 py-2">ADMINISTRADOR</span>
                                                <% } else { %>
                                                    <span class="badge bg-secondary fs-6 px-3 py-2">EMPLEADO</span>
                                                <% } %>
                                            </div>
                                        </div>

                                        <div class="mb-3">
                                            <label class="small text-muted text-uppercase fw-bold">Base / Concesionario</label>
                                            <% if(usuarioDetalle.nombre_concesionario) { %>
                                                <p class="fs-5 mb-0 text-dark fw-bold"><%= usuarioDetalle.nombre_concesionario %></p>
                                                <small class="text-muted"><i class="bi bi-geo-alt"></i> <%= usuarioDetalle.ciudad_concesionario %></small>
                                            <% } else { %>
                                                <p class="fs-5 text-muted font-italic">Sin asignación</p>
                                            <% } %>
                                        </div>

                                        <div class="mt-4 pt-3 border-top">
                                            <button class="btn btn-outline-dark btn-sm disabled" disabled>
                                                <i class="bi bi-pencil-square"></i> Editar Datos
                                            </button>
                                            <small class="d-block mt-2 text-muted" style="font-size: 0.75rem;">
                                                * Para modificar tus datos sensibles o cambiar de base, contacta con un administrador.
                                            </small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </div>
                </div>
            </main>
        </div>
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>