<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>Resultados Importación</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body class="bg-light">
    <%- include('./barraNavegadora.ejs') %>
    <div class="container my-5 text-center">
        
        <div class="card shadow-sm border-success border-top border-4 d-inline-block text-start p-4" style="min-width: 50%;">
            <div class="text-center mb-4">
                <i class="bi bi-check-circle-fill text-success display-1"></i>
                <h2 class="mt-3">Proceso Finalizado</h2>
            </div>

            <ul class="list-group list-group-flush fs-5">
                <li class="list-group-item d-flex justify-content-between">
                    <span>Vehículos añadidos:</span>
                    <span class="badge bg-success rounded-pill"><%= logs.insertados %></span>
                </li>
                <li class="list-group-item d-flex justify-content-between">
                    <span>Vehículos actualizados:</span>
                    <span class="badge bg-primary rounded-pill"><%= logs.actualizados %></span>
                </li>
            </ul>

            <% if(logs.errores.length > 0) { %>
                <div class="alert alert-danger mt-4">
                    <strong>Errores encontrados:</strong>
                    <ul class="mb-0 small">
                        <% logs.errores.forEach(err => { %>
                            <li><%= err %></li>
                        <% }) %>
                    </ul>
                </div>
            <% } %>

            <div class="text-center mt-4">
                <a href="/admin/vehicles" class="btn btn-dark">Ir a Gestión de Flota</a>
            </div>
        </div>

    </div>
</body>
</html>