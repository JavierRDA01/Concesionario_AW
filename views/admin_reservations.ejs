<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <title>Gestión de Reservas - Admin</title>
</head>
<body class="bg-light">
    
    <%- include('./barraNavegadora.ejs') %>

    <div class="container-fluid my-4">
        <div class="row">
            <div class="col-md-2 d-none d-md-block bg-white sidebar p-3 shadow-sm rounded me-3" style="min-height: 80vh;">
                <h5 class="text-muted">Menú Admin</h5>
                <hr>
                <ul class="nav flex-column">
                    <li class="nav-item mb-2">
                        <a href="/admin/dashboard" class="nav-link text-dark">
                            <i class="bi bi-speedometer2"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/admin/vehicles" class="nav-link text-dark">
                            <i class="bi bi-car-front"></i> Gestión de Flota
                        </a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/admin/dealerships" class="nav-link text-dark">
                            <i class="bi bi-building"></i> Concesionarios
                        </a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/admin/users" class="nav-link text-dark">
                            <i class="bi bi-people"></i> Usuarios
                        </a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/admin/reservations" class="nav-link active text-danger fw-bold bg-light rounded">
                            <i class="bi bi-calendar-check"></i> Reservas
                        </a>
                    </li>
                </ul>
            </div>

            <main class="col-md-9 ms-sm-auto col-lg-9 px-md-4">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2 class="text-dark">Listado de Reservas</h2>
                </div>
                
                <div class="card shadow-sm">
                    <div class="card-body p-0">
                        <div class="table-responsive">
                            <table class="table table-hover table-striped mb-0 align-middle" style="table-layout: fixed; width: 100%;">
                                <thead class="bg-dark text-white">
                                    <tr>
                                        <th style="width: 5%;">ID</th>
                                        <th style="width: 15%;">Usuario</th>
                                        <th style="width: 20%;">Vehículo</th>
                                        <th style="width: 20%;">Fechas y Horas</th>
                                        <th style="width: 10%;">Estado</th>
                                        <th style="width: 30%;">Incidencias</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if(reservas.length === 0) { %>
                                        <tr><td colspan="6" class="text-center py-3">No hay reservas registradas.</td></tr>
                                    <% } else { %>
                                        <% reservas.forEach(r => { %>
                                        <tr>
                                            <td class="text-truncate"><strong>#<%= r.id_reserva %></strong></td>
                                            <td class="text-truncate" title="<%= r.nombre_usuario %>">
                                                <span class="fw-bold text-dark"><%= r.nombre_usuario %></span>
                                            </td>
                                            <td class="text-truncate" title="<%= r.vehiculo %>">
                                                <span class="text-muted"><%= r.vehiculo %></span>
                                            </td>
                                            <td>
                                                <div class="d-flex flex-column small">
                                                    <div class="mb-1 text-truncate">
                                                        <span class="badge bg-light text-secondary border me-1">INICIO</span>
                                                        <strong>
                                                            <%= new Date(r.fecha_inicio).toLocaleString('es-ES', {
                                                                day: '2-digit', month: '2-digit', year: 'numeric', 
                                                                hour: '2-digit', minute: '2-digit'
                                                            }) %>
                                                        </strong>
                                                    </div>
                                                    <div class="text-truncate">
                                                        <span class="badge bg-light text-secondary border me-1" style="width: 48px;">FIN</span>
                                                        <strong>
                                                            <%= new Date(r.fecha_fin).toLocaleString('es-ES', {
                                                                day: '2-digit', month: '2-digit', year: 'numeric', 
                                                                hour: '2-digit', minute: '2-digit'
                                                            }) %>
                                                        </strong>
                                                    </div>
                                                </div>
                                            </td>
                                            <td>
                                                <% if(r.estado === 'activa') { %>
                                                    <span class="badge bg-success">Activa</span>
                                                <% } else if(r.estado === 'finalizada') { %>
                                                    <span class="badge bg-secondary">Finalizada</span>
                                                <% } else { %>
                                                    <span class="badge bg-danger"><%= r.estado %></span>
                                                <% } %>
                                            </td>
                                            <td class="text-break">
                                                <% if(r.incidencias_reportadas && r.incidencias_reportadas.trim() !== '') { %>
                                                    <div class="alert alert-warning p-2 mb-0 small d-flex align-items-start">
                                                        <i class="bi bi-exclamation-triangle-fill me-2 mt-1 flex-shrink-0"></i>
                                                        <span><%= r.incidencias_reportadas %></span>
                                                    </div>
                                                <% } else { %>
                                                    <span class="text-muted small font-italic">Sin incidencias</span>
                                                <% } %>
                                            </td>
                                        </tr>
                                        <% }) %>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>